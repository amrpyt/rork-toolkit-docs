# Deprecated

Use the current [Security and production readiness guide](../docs/08-security-production.md). The measured public endpoints did not require visible authentication at test time, but production integrations must still use an internal authenticated gateway.
